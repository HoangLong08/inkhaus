/**
 * `/admin/catalog/*` response shapes - products, colours, sizes, price tiers.
 * The options lookup (`catalogOptionsSchema`) lives in `lookups.ts`.
 *
 * Empty until the catalog workstream fills it in. Prefix every export
 * (`catalog…`) - `index.ts` re-exports every file here with `export *`, so a
 * name two files share fails typecheck.
 */
export {};
