/**
 * `GET|PATCH /admin/customers[/:id]` response shapes.
 *
 * Empty until the customers workstream fills it in. Prefix every export
 * (`customer…`) - `index.ts` re-exports every file here with `export *`, so a
 * name two files share fails typecheck.
 */
export {};
