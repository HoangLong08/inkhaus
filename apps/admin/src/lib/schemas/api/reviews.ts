/**
 * `/admin/reviews` response shapes. The status enum (`reviewStatusSchema`) is in
 * `core.ts`.
 *
 * Empty until the reviews workstream fills it in. Prefix every export
 * (`review…`) - `index.ts` re-exports every file here with `export *`, so a
 * name two files share fails typecheck.
 */
export {};
