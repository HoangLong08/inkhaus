/**
 * Catalog writes with their authorization attached - including the per-field
 * price rule (`catalog.price`). Empty until the catalog workstream fills it in.
 * Prefix every export (`applyProduct…`, `applyColor…`, …).
 */
export {};
