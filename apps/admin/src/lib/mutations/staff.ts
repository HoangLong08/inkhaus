/**
 * Staff writes with their authorization attached - `staffChangeError` from
 * `@inkhaus/shared/admin` before the round trip. Empty until the staff
 * workstream fills it in. Prefix every export (`applyStaff…`).
 */
export {};
