/**
 * `/admin/staff` calls. Empty until the staff workstream fills it in;
 * `index.ts` already mounts it as `adminApi.staff`. The assignee directory is
 * `adminApi.lookups.staffDirectory`.
 */
export const staffApi = {};
