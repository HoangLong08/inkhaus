/**
 * `/admin/catalog/*` calls. Empty until the catalog workstream fills it in;
 * `index.ts` already mounts it as `adminApi.catalog`. The options lookup is
 * `adminApi.lookups.catalogOptions`.
 */
export const catalogApi = {};
